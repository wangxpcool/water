# water-web

Vue 3 + Vite frontend for the personal asset management system.

## Run

Install dependencies:

```bash
npm install
```

Start dev server:

```bash
npm run dev
```

The Vite dev server proxies `/api/*` to `http://localhost:8080`, so start the Spring Boot backend first.
